{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for ÖppenBokföring.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:allow-app-read",
    "fs:allow-app-write",
    "fs:allow-app-meta"
  ]
}

